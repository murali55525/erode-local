import React from "react";
import ChatWidget from "../components/ChatWidget";

const HomePage = () => {
  return (
    <div>
      {/* ...existing code... */}
      {/* Integrate ChatWidget for "Chat with us" functionality */}
      <ChatWidget />
    </div>
  );
};

export default HomePage;
